# Настройка Supabase для системы снимков данных

## 🎯 Зачем нужен Supabase?

Supabase обеспечивает:
- ✅ **Надежное хранение** снимков данных в облаке
- ✅ **Доступ с любого устройства** к историческим данным
- ✅ **Масштабируемость** для больших объемов данных
- ✅ **Бэкапы** и восстановление данных

## 📋 Пошаговая настройка

### 1. Создание аккаунта Supabase

1. Перейдите на https://supabase.com
2. Нажмите "Start your project"
3. Зарегистрируйтесь (можно через GitHub)

### 2. Создание проекта

1. Нажмите "New Project"
2. Выберите организацию (или создайте новую)
3. Заполните данные проекта:
   - **Name**: `crm-analytics`
   - **Database Password**: создайте надежный пароль
   - **Region**: выберите ближайший к вам регион
4. Нажмите "Create new project"
5. Дождитесь создания проекта (1-2 минуты)

### 3. Получение ключей доступа

1. В созданном проекте перейдите в "Settings" → "API"
2. Скопируйте:
   - **Project URL** (например: `https://abcdefgh.supabase.co`)
   - **anon public key** (длинный ключ начинающийся с `eyJ`)

### 4. Создание файла окружения

Создайте файл `.env.local` в корне проекта:

```bash
# В корне проекта создайте файл .env.local
VITE_SUPABASE_URL=https://ваш-проект-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=ваш_публичный_ключ_supabase
```

**⚠️ Важно:** Замените значения на ваши реальные ключи из Supabase!

### 5. Запуск миграций

После настройки ключей выполните:

```bash
# Перезапустите сервер разработки
npm run dev
```

Миграции запустятся автоматически при первом обращении к базе данных.

### 6. Проверка подключения

1. Откройте приложение
2. Перейдите в Bitrix24 → Настройки
3. Нажмите "Загрузить сделки"
4. Если все настроено правильно, в консоли будет:
   ```
   ✅ Снимок сохранен в Supabase
   ```

## 🔧 Альтернативное решение (без Supabase)

Если не хотите настраивать Supabase сейчас, система будет автоматически использовать **локальное хранение браузера**:

- ✅ Снимки сохраняются в localStorage
- ✅ Доступны только в текущем браузере
- ⚠️ Ограничено 10 последними снимками
- ⚠️ Данные могут быть потеряны при очистке браузера

В консоли будет сообщение:
```
⚠️ Supabase недоступен, используем локальное хранение
✅ Локальный снимок создан: snapshot_1234567890
```

## ❓ Решение проблем

### Проблема: "Could not find the table 'public.data_snapshots'"

**Решение:**
1. Убедитесь, что файл `.env.local` создан правильно
2. Проверьте, что ключи Supabase корректны
3. Перезапустите сервер разработки: `npm run dev`

### Проблема: "Invalid API key"

**Решение:**
1. Проверьте Project URL и API Key в Supabase
2. Убедитесь, что используете **anon public key**, а не service role key
3. Перезапустите приложение

### Проблема: Медленное подключение

**Решение:**
1. Выберите регион Supabase ближе к вашему местоположению
2. Проверьте интернет-соединение

## 📊 Что будет храниться в Supabase?

```sql
-- Структура таблицы data_snapshots
CREATE TABLE data_snapshots (
    id UUID PRIMARY KEY,
    created_at TIMESTAMP WITH TIME ZONE,
    week_start DATE,
    week_end DATE,
    deals_count INTEGER,
    tasks_count INTEGER,
    deals_data JSONB,  -- Все сделки из Bitrix24
    tasks_data JSONB,  -- Все задачи из Bitrix24
    metadata JSONB     -- Источник, версия, webhook URL
);
```

**Преимущества JSONB:**
- Быстрый поиск по данным
- Сжатие больших объемов
- Индексация для производительности

## 🚀 Готово!

После настройки Supabase система будет:
- Автоматически создавать снимки при загрузке данных
- Хранить историю по неделям
- Позволять просматривать данные за любой период
- Обеспечивать надежное хранение в облаке