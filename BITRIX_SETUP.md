# Настройка интеграции с Bitrix24

## 🎯 Как настроить получение данных из Bitrix24

### 1. Создание входящего вебхука в Bitrix24

1. **Войдите в ваш Bitrix24**
2. **Перейдите в "Разработчикам" → "Другие" → "Входящий вебхук"**
3. **Создайте новый вебхук** со следующими правами:
   - ✅ `crm` - доступ к CRM (сделки, контакты, компании)
   - ✅ `task` - доступ к задачам
   - ✅ `user` - доступ к пользователям
4. **Скопируйте URL вебхука** (например: `https://ваш-домен.bitrix24.ru/rest/1/ваш-код/`)

### 2. Настройка в приложении

1. Запустите приложение
2. Перейдите в **Bitrix24 → Настройки**
3. Вставьте URL вебхука в поле "Webhook URL"
4. Нажмите **"Сохранить"**

### 3. Загрузка данных

**Сделки:**
- Нажмите **"Загрузить сделки"**
- Система автоматически найдет воронку "ПРОДАЖИ"
- Загрузит все сделки с пользовательскими полями
- Создаст снимок данных на текущую неделю

**Задачи:**
- Нажмите **"Загрузить задачи"**
- Система загрузит все задачи с полной информацией
- Создаст снимок задач на текущую неделю

## 📊 Что загружается из Bitrix24

### Сделки (crm.deal.list):
```javascript
{
  "ID": "123",
  "TITLE": "Название сделки",
  "STAGE_ID": "C1:NEW",
  "OPPORTUNITY": "100000",
  "CURRENCY_ID": "RUB",
  "ASSIGNED_BY_ID": "1",
  "CONTACT_ID": "456",
  "COMPANY_ID": "789",
  "DATE_CREATE": "2024-01-01T12:00:00+03:00",
  "UF_CRM_XXXXXXXX": "Пользовательские поля",
  // ... все остальные поля
}
```

### Задачи (task.item.list):
```javascript
{
  "ID": "101",
  "TITLE": "Название задачи",
  "STATUS": "2",
  "PRIORITY": "1",
  "CREATED_BY": "1",
  "RESPONSIBLE_ID": "2",
  "CREATED_DATE": "2024-01-01T12:00:00+03:00",
  "DESCRIPTION": "Описание задачи"
}
```

### Дополнительные данные:
- **Пользователи** - имена и фамилии для всех ID
- **Контакты** - информация о клиентах
- **Компании** - информация об организациях
- **Стадии воронки** - с цветами и названиями
- **Метаданные полей** - расшифровка списочных значений

## 🔧 Особенности работы

### Воронка "ПРОДАЖИ"
Система автоматически находит воронку с названием, содержащим "продаж" (регистронезависимо). Если такой воронки нет, будет использована основная воронка (ID: 0).

### Пользовательские поля (UF_CRM_*)
Все пользовательские поля загружаются с автоматической расшифровкой:
- Списочные поля преобразуются из ID в читаемый текст
- Связи с контактами и компаниями разрешаются в названия
- Множественные значения объединяются через запятую

### Пагинация
Система автоматически обрабатывает большие объемы данных:
- Сделки загружаются пакетами по 50
- Задачи загружаются пакетами по 50
- Контакты и компании - по 25 в пакете

## ⚡ Оптимизация производительности

### Кеширование
Все загруженные данные кешируются в приложении до следующего обновления.

### Снимки данных
При каждой загрузке создается снимок на текущую неделю:
- В Supabase (если настроен)
- В localStorage (как fallback)
- Максимум 10 локальных снимков

### Логирование
Вся информация о загрузке выводится в консоль браузера:
```javascript
console.log("📊 Загружено сделок: 1,234");
console.log("📋 Загружено задач: 567");
console.log("✅ Снимок создан: snapshot_20240101");
```

## 🛠 Возможные проблемы

### "Ошибка подключения к Bitrix24"
1. Проверьте URL вебхука
2. Убедитесь, что вебхук имеет нужные права
3. Проверьте доступность вашего Bitrix24

### "Не найдена воронка ПРОДАЖИ"
1. Проверьте название воронки в настройках CRM
2. Убедитесь, что у вебхука есть права на чтение воронок
3. Система автоматически использует основную воронку

### Медленная загрузка
1. Нормально для больших объемов данных (>1000 записей)
2. Следите за прогрессом в консоли
3. Не закрывайте браузер во время загрузки

## 📈 Рекомендации

1. **Регулярность обновления:** Загружайте данные еженедельно для актуальности
2. **Мониторинг:** Следите за логами в консоли браузера
3. **Резервирование:** Настройте Supabase для надежного хранения снимков
4. **Производительность:** При >10,000 сделок рассмотрите фильтрацию по датам